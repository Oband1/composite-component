[project]
name = "composite-component"
authors = ["AI Developer"]
description = "A decentralized component system for tracking and verifying state transitions"
telemetry = false
requirements = []
[contracts.celestial-registry]
path = "contracts/celestial-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
